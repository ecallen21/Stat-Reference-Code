# Location-scale families
# R: `stats::dnorm/rnorm/dlnorm/dcauchy/dlaplace` (via `rmutil`),
#    `EnvStats::eloglik` — general location-scale MLE.
# Python: `scipy.stats` distributions all take `loc` / `scale`.
